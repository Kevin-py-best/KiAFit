# Database migrations

Place versioned `node-pg-migrate` migrations here. The initial migration should enable PostGIS before creating KiaStop and check-in tables.

Do not add student data or production credentials to migrations.
